-- Migration template for database changes

-- Add your migration SQL statements here

-- Example:
-- ALTER TABLE users ADD COLUMN last_login TIMESTAMP;

-- Use TypeORM CLI to generate and run migrations
-- Command: typeorm migration:generate -n MigrationName
-- Command: typeorm migration:run
